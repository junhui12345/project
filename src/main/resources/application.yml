spring:
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true  # 추가
  datasource:
    url: jdbc:h2:file:./data/project
    username: sa
    password:
    driverClassName: org.h2.Driver